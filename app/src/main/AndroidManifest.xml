<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.fh.swengb.loggingviewsandactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HigherOrderActivityy"></activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".HigherOrderActivity" />
        <activity
            android:name=".LessonRatingActivity"
            android:parentActivityName=".LessonListActivity" />
        <activity
            android:name=".LessonListActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".LessonRating" />
        <activity android:name=".RatingActivity" />
        <activity android:name=".ViewsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>